[build]
  command = "composer install --ignore-platform-reqs && php export.php && npm install && npm run build"
  publish = "public"

[build.environment]
  PHP_VERSION = "8.2"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
